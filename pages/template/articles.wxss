/***template 的样式 start （这里为了能全局使用，不在所有用到模版的页面都引用一次，将改样式的引用放到了app.wxss中）***/
/**文章的信息的容器**/
.article-info-container{
  display:flex;
  flex-direction:row;
  justify-content:space-between;
  height:160rpx;
  width:100%;
}

/**文章题目和信息的容器**/
.article-container {
  width:460rpx;
  display:flex;
  flex-direction:column;
  justify-content:center;
  margin-left:40rpx;
}

/**文章题目**/
.title {
  font-size:40rpx;
  font-weight:bold;
  color:#000000;
}

/**文章作者和朝代**/
.author {
  font-size:36rpx;
  color:#4A4A4A;
}

/**分隔线**/
.split-line {
  width:100%;
  height:2rpx;
  background-color:#2CAEE9;
}

/**图标的容器**/
.icon-container {
  width:80rpx;
  display:flex;
  flex-direction:column;
  justify-content:center;
  align-items:center;
}

/**几个小图标的样式，主要只是定义了一下大小**/
.icon {
  width:48rpx;height:48rpx;
}

/***template 的样式 end***/